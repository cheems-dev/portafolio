import styled from "styled-components";

import { TemplateContainerStyledProps as Props } from "./TemplateContainer.types";

const TemplateContainerStyled = styled.div<Props>`
  .TemplateContainer {
  }
`;

export default TemplateContainerStyled;
